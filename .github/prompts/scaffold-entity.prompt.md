---
description: "Generate an EF Core entity with configuration, migration, and repository pattern."
agent: "backend-dev"
argument-hint: "Describe the entity (name, properties, relationships)"
---

Create an EF Core entity based on this specification. Include entity class, `IEntityTypeConfiguration<T>`, DbSet registration, and migration command.

## Specification
{{input}}
