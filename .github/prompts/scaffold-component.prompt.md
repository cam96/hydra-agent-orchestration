---
description: "Generate a Blazor component with code-behind, CSS isolation, and parameter API."
agent: "frontend-dev"
argument-hint: "Describe the component (name, purpose, parameters, events)"
---

Create a Blazor component based on this specification. Include `.razor`, `.razor.cs`, and `.razor.css` files following project conventions.

## Specification
{{input}}
